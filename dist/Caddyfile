:2015 {
  tls off
  root /usr/share/markco
  rewrite {
    to {path} /
  }
  templates
  errors stderr
}

:2015/api {
  tls off
  proxy / localhost:{$MARKCO_PORT} {
    without /api
  }
  errors stderr
}

:2015{$MARKCO_DATA_URI} {
  tls off
  root {$MARKCO_DATA_DIR}
  errors stderr
}
